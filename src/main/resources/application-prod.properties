# PRODUCTION ENVIRONMENT (PostgreSQL / Neon)

spring.datasource.url=jdbc:postgresql://ep-fragrant-butterfly-azqf0pkw-pooler.c-3.ap-southeast-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require
spring.datasource.username=${DATABASE_USERNAME}
spring.datasource.password=${DATABASE_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Untuk production, update adalah standar untuk MVP
spring.jpa.hibernate.ddl-auto=update

# Matikan logging SQL di production untuk performa
spring.jpa.show-sql=false
